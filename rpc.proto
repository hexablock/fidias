syntax = "proto3";

package fidias;

import "github.com/hexablock/hexalog/rpc.proto";

service FidiasRPC {
    rpc GetKeyRPC(KeyValuePair) returns (KeyValuePair) {}
}

message KeyValuePair {
    bytes Key = 1;
    bytes Value = 2;
    hexalog.Entry Entry = 3;
}
